# Vagrant Setup

### Initialize the VMs
```
    vagrant up --provision 
```
### SSH in the provisioned VMs
```
    vagrant ssh kube1
    vagrant ssh kube2
    vagrant ssh kube3
```
### Destroy the VMs
```
    vagrant destroy -f 
```
